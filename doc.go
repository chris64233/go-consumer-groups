// Package consumergroups 提供消费组协调服务：以单调递增的分配版本
// （generation）隔离新旧成员操作，管理组成员、确定性分区分配、
// 会话超时与位点提交，并支持状态持久化。
//
// 核心不变量：
//   - 每次成员变化（加入/离开/超时剔除）都使分配版本 +1，并整体发布
//     一份新的完整分区分配，同一版本内一个分区至多归属一个成员；
//   - 携带旧分配版本的心跳与位点提交一律被拒绝；
//   - 只有当前分区所有者能提交位点，位点默认单调递增；
//   - 幂等请求号去重提交，同请求号不同内容构成冲突。
package consumergroups
